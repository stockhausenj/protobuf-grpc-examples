# dns-service

project features
- Use Buf CLI to generate gRPC interfaces.
- Use Buf CLI to both lint and detect breaking changes in the protobufs.

design features
- Protobufs for serialization and deserialization of data.
- gRPC for performance and type safety.

functionality
- manage root DNS servers
- resolve any DNS record via root DNS servers
